#include <bits/stdc++.h>
#include <string>
#include <vector>
#include <iostream>
int main()
{
    int n;
    std::cin >> n;
    std::vector<string> s;
    // for(int i = 0; i < n; i++)std::cin >> s.at(i);

    // std::cout << "------" << std::endl;
    // std::cout << n << std::endl;
    // for(int i = 0; i < n; i++)std::cout << s[n] << std::endl;


    return 0;
}